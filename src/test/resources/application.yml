# 스프링 부트에서 기본적으로 테스트 용 설정을 만들어주기 때문에
# 아래 설정들을 모두 지워도 된다.
#
#spring:
#  datasource:
#    url: jdbc:h2:mem:test # 테스트 용 in-memory 데이터베이스
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
